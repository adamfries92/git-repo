const MAX_POINTS: u32 = 100_000;

fn main() {
    let mut x = 5;
    println!("x = {}", x);

    x = 6;
    println!("x = {}", x);

    println!("MAX_POINTS / x = {}", MAX_POINTS / x);

    let y = 5;

    let y = y + 1;

    let y = y * 2;

    println!("y = {}", y);
}
